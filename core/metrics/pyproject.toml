[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "gaia-metrics"
version = "0.1.0"
description = "Gaia metrics and logging utilities"
requires-python = ">=3.11"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["src/gaia_metrics"]

